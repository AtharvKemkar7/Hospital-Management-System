<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false">

    <!--
      Structured JSON logging. Every log line carries:
        - service.name (from spring.application.name via property)
        - correlationId (from MDC, set by CorrelationIdFilter)
      Sensitive fields (passwords, JWTs, refresh tokens) are NEVER logged.
    -->

    <springProperty scope="context" name="serviceName" source="spring.application.name" defaultValue="auth-service"/>

    <appender name="JSON_STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <includeMdcKeyName>correlationId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <customFields>{"service.name":"${serviceName}"}</customFields>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <thread>thread</thread>
                <level>level</level>
            </fieldNames>
        </encoder>
    </appender>

    <!-- In local profile, also write a human-readable pattern to the console
         for easier reading while developing. -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%X{correlationId:-no-correlation}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <springProfile name="local | dev | test">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <springProfile name="!local &amp; !dev &amp; !test">
        <root level="INFO">
            <appender-ref ref="JSON_STDOUT"/>
        </root>
    </springProfile>

</configuration>
